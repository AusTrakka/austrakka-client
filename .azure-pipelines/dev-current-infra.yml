trigger:
  branches:
    include:
      - Development

pr: none

pool:
  vmImage: windows-latest

steps:
  - template: templates/build.yml
  - template: templates/deploy.yml
    parameters:
      webAppName: 'app-autrnewclient-dev-ase-01'

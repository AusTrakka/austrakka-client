trigger:
  branches:
    include:
      - Development

pr: none

pool:
  vmImage: ubuntu-latest

steps:
  - template: templates/build.yml
  - template: templates/deploy.yml
    parameters:
      environment: 'dev'
